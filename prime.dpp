fn prime(n)
{
	let m = 3;
	let k = n / 2;

	while (m < k)
	{
		if (n % m == 0)
		{
			return 0;
		}

		m = m + 2;
	}

	return 1;
}

fn main()
{
	let n = 3;
	let max = 100000;

	print(2);

	while (n < max)
	{
		if (prime(n))
		{
			print(n);
		}

		n = n + 2;
	}
}
